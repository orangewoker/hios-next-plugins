# 海报编辑器

HIOS Next 的插件式轻量海报编辑器示例。插件同时贡献：

- 应用板块中的「海报编辑器」应用
- 画布中的「海报编辑器」节点

编辑器使用 Canvas 2D 渲染，项目状态通过 `hios-plugin-app/v1` 持久化到宿主；节点使用 `hios-plugin-node/v1` 输出 PNG 图片。

开发时可在 HIOS 的「插件管理 → 安装第三方插件 → 本地插件」选择本目录安装。安装后进入「应用」即可看到海报编辑器，画布节点列表中也会出现同名节点。
